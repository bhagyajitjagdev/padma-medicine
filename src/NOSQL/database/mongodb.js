const models = {};

// MODELS
models.User = require("../models/user.model");
models.Medicine = require("../models/medicine.model");
models.Inventory = require("../models/inventory.model");
models.Sale = require("../models/sale.model");

module.exports = models;
